/* ============================================================
   Sites managed by Broadreach but not (yet) monitored via AMMP.
   Placeholder list for the "Not on AMMP" section on Portfolio
   Overview — edit this array directly, no code changes needed.
   Each entry: { name: 'Site name', url: 'https://external-platform...' }
   ============================================================ */

const OFF_AMMP_SITES = [
  // { name: 'Example Site', url: 'https://example-monitoring-platform.com/site/123' },
];

Object.assign(window, { OFF_AMMP_SITES });
