{
  "name": "ammp-proxy",
  "version": "1.1.0",
  "description": "Broadreach AMMP monitoring — CORS proxy for the AMMP Data API plus the dashboard pages, served from one origin.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
